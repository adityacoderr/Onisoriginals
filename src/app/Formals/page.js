import React from 'react'
import Products from '../Components/Products'

const page = () => {
    return (
        <>
            <Products heading={"FORMALS"} />
            <Products heading={" "} />
        </>
    )
}

export default page
